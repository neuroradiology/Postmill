{{ form_start(form, {attr: {class: 'vp vp--guttered'}}) }}
  {% if form.title is defined %}
    {{ form_row(form.title, {attr: {rows: 2}}) }}
  {% endif %}

  {{ form_row(form.body, {attr: {rows: 6}}) }}

  {{ form_row(form.submit) }}
{{ form_end(form) }}
